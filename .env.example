# Environment Configuration for propsdb

# Server Configuration
PORT=3000

# Database Configuration
DB_TYPE=mysql  # Options: mysql, mariadb, postgres, postgresql, sqlite, sqlserver, mssql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=jam_build

# Application Database User (for app data operations)
DB_APP_USER=jbadmin
DB_APP_PASSWORD=your_app_password
DB_APP_CONNECTION_LIMIT=5

# User Database User (for user data operations)
DB_USER=jbuser
DB_PASSWORD=your_user_password
DB_CONNECTION_LIMIT=5

# Authorizer Configuration
AUTHZ_URL=http://localhost:8080
AUTHZ_CLIENT_ID=your_client_id
