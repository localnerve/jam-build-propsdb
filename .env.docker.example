# Docker Compose Environment Variables
# Copy this file to .env.docker and customize

# Authorizer Configuration
AUTHZ_CLIENT_ID=your_authorizer_client_id
ADMIN_SECRET=change_this_to_a_secure_random_string

# Database Passwords (change these in production)
MYSQL_ROOT_PASSWORD=rootpassword
DB_APP_PASSWORD=admin_password
DB_USER_PASSWORD=user_password
